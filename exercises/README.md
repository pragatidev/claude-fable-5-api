# Exercises

`ex_parse_stop_reason.py` is the S2 coding exercise. Implement `get_text_or_raise`.

```bash
pytest tests/test_exercise.py
```

The starter must fail. The file in `solutions/` must pass. That is the auto-grader contract.
