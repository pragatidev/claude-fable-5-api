# Agent notes for this repo

- Default model for live labs is `claude-fable-5`.
- Do not pass `thinking: {type: "disabled"}`. It returns 400.
- Check `stop_reason` before reading `content`.
- Mythos 5 is not a lab.
- Re-read `docs/CURRENCY.md` before changing IDs, betas, or rates.
- Tests must stay green without `ANTHROPIC_API_KEY`.
