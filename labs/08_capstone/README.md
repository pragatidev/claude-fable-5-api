# Capstone: a client that does not crash

1. Open `starter/client.py`.
2. Implement `handle` and `fallback_body` using `fable_client`.
3. Run:

```bash
pytest tests/test_capstone.py
```

The suite loads fixtures only. No API key.

`solution/client.py` is the reference. Use it after you have tried.
